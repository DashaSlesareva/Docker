version: '3.7'
services:
  postgres:
    image: postgres:12-alpine
    ports:
      - '5432:5432'
#    volumes:
#      - ./data:/var/lib/mysql
    environment:
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=iiH2eex4aeweiBo4
      - POSTGRES_PORT=5432
